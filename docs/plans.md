# Plans

現在TDDの対象としているtodo（作業用・使い捨て）。

## その他

- [x] テストに登場する文字列を日本語から英語にする
- [ ] HTMLエスケープ
- [ ] アイテム行内のMarkdown対応
- [ ] 拡張機能のアイコン作成
- [ ] .mdファイルを右クリックで「Preview as User Story Map」を選択可能にする
- [x] 再描画されてもズーム値が維持される
  - [x] Webviewからズーム値の変更が通知された後にドキュメントを編集すると、再描画後のHTMLにもそのズーム値が反映されている
  - [x] ズーム値の通知を受けていなければ、再描画後のズームは等倍のまま（記録テスト）
  - [x] renderMapにズーム値を渡すと、マップ要素がそのズーム値で描画される
  - [x] renderMapにズーム値を渡さないと等倍で描画される
  - [x] ズームボタンを押すと、Webviewが新しいズーム値を拡張機能へ通知する（記録テスト＋明白な実装。手動確認が必要）
  - [x] Webviewスクリプトの初期ズーム値が、描画時に渡されたズーム値と一致している（ズーム後の再描画で＋を押すと1.2倍に戻る不整合を防ぐ。処理をwebview/main.tsへ移したためrenderMapのテストは削除し、webview.test.tsに記録）
  - [x] 2つのドキュメントをそれぞれプレビューし、片方のズーム値を変更しても、もう片方の再描画後のズームは変わらない（記録テスト）
